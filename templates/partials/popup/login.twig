<div id="small-dialog-login" class="zoom-anim-dialog mfp-hide">
    <div class="main-content d-flex flex-column">
        <h4 >Вход на сайт</h4>
        <div class="col-md-12">
            <div class="dashboard-content px-3 pt-4">
                <form action="/api/auth/login" method="POST" data-form-type="login">
                    <div class="mb-3">
                        <label for="email" class="form-label">Адрес электронной почты</label>
                        <input type="email" name="email" class="form-control" id="email" placeholder="Email адрес">
                    </div>
                    <div class="mb-3">
                        <label for="password" class="form-label">Пароль</label>
                        <input type="password" name="password" class="form-control" id="password" placeholder="Пароль">
                    </div>
                    <input type="hidden" name="csrf_token" value="{{ csrf_token }}">
                    <div class="mb-3">
                        <input type="checkbox" name="remember_me" id="remember_me" >
                        <label for="remember_me">Запомнить меня</label>
                    </div>
                    <div class="col-auto">
                        <button type="submit" id="doLogin" class="btn btn-primary mb-3">Войти</button>
                    </div>
                </form>
                <p>Или войти через:</p>
                <div class="social-auth d-flex">
                    <a href="/auth/google" class="btn btn-google">
                        <svg width="18" height="18" fill="currentColor">
                            <use xlink:href="#icon-google"></use>
                        </svg> Google</a>
                    <a href="/auth/yandex" class="btn btn-yandex">
                        <svg width="18" height="18" fill="currentColor">
                            <use xlink:href="#icon-yandex"></use>
                        </svg> Яндекс
                    </a>
                </div>
                <p>
                    <a class="register-link" href="#">Зарегистрироваться</a> | <a class="reset-link" href="#">Забыли пароль?</a>
                </p>
            </div>
        </div>
    </div>
</div>
