{% extends '/layouts/email.twig' %}

{% block content %}
    <div class="header">
        <h1>{{ site_name }}</h1>
    </div>

    <div class="content">
        <h2>{{ subject }}</h2>

        <div class="main-content">
                {# Основное содержимое письма #}
                {{ text|raw }}
        </div>
    </div>

{% endblock %}

