{% extends '/layouts/base.twig' %}

{% block title %}
    {{ seo_title()}}
{% endblock %}
{% block description %}
    {{ seo_description() }}
{% endblock %}

{% block keywords %}
    {{ seo_keywords()}}
{% endblock %}

{% block content %}
<div class="dashboard-content px-3 pt-4" data-page="home">
    <h1 class="fs-3">Образцы документов по управлению персоналом (HR)</h1>
    <div class="col-md-10 px-3 pt-4">    <p>
            <b>Собрали для вас коллекцию шаблонов документов для управления персоналом (HR-менеджмент).</b>
        </p>
        <p>
            Для помощи в работе специалистам по управлению персоналам (Эйчарам) нашей командой создана база документов по кадровому делопроизводству (приказы, заявления, трудовые договора, должностные инструкции и прочие документы). Все приведенные на сайте документы соответствуют требованиям нормативно-правовых актов РФ и являются актуальными.
        </p>
        <p>
            Для быстрого поиска документов по базе данных вы можете воспользоваться поиском расположенным ниже.
        </p>
        <p>
            Справа расположены разделы сайта, где вы можете подобрать для себя необходимый образец документа.
        </p>
        <h3 class="fs-4">Поиск документа</h3>
        <p>Введите ключевые слова (например, "трудовой договор" или "прием на работу"), и мы найдем подходящие документы. Вы также можете выбрать категорию для удобного просмотра. Поиск ограничен 20 результатами.</p>
        <form id="search-form" data-form-type="client-only">
            <div class="input-group mt-3">
                <label for="search-input"></label>
                <input type="text" id="search-input" class="form-control" placeholder="Введите поисковый запрос">
                <button type="submit" class="btn btn-primary">Искать</button>
            </div>
        </form>
    </div>

    <div class="col-md-10 px-3 pt-4" id="search-result">

    </div>
    <div class="col-md-12 px-3 pt-4">
        <h3>Добавленные документы:</h3>
        <div class="d-flex flex-wrap">
            {% for document in documents %}
                <div class="card m-2 shadow-sm" style="width: 20rem; border-radius: 8px; transition: transform 0.2s;">
                    <div class="card-body">
                        <h6 class="card-title">
                            <a href="/document/{{ document.id }}" class="text-decoration-none text-dark font-weight-bold">
                                {{ document.title }}
                            </a>
                        </h6>
                        <span class="badge bg-light text-dark mb-2" style="font-size: 0.8rem;">
                        {{ document.section_name }}
                        </span>
                        <span class="badge bg-light text-dark mb-2" style="font-size: 0.8rem;">
                        {{ document.type_name }}
                        </span>
                        <div class="d-flex justify-content-between align-items-center mt-3">
                            <a href="/document/{{ document.id }}" class="btn btn-sm btn-outline-primary">Подробнее</a>
                            <small class="text-muted">{{ document.size }} kb</small>
                            <small class="text-muted">{{ document.mime_type }}</small>
                        </div>
                    </div>
                </div>
            {% endfor %}
        </div>

    </div>

</div>

{% endblock %}
