{% extends 'layouts/base.twig' %}

{% block title %}
    Шаблоны документов: {{ direction.name }}
{% endblock %}
{% block description %}
    Подборка актуальных шаблонов (образцов) документов по разделу работы {{ direction.name }}
{% endblock %}
{% block keywords %}
{% endblock %}
{% block content %}
    <div class="dashboard-content px-3 pt-4" data-page="documents-page">
        <h1 class="fs-3">{{ direction.name }}</h1>
        <p class="mt-3">
            Ниже вы найдете образцы и шаблоны документов по ведению кадрового документаоборота. Ниже приведена статическая страница с документами, если вам необходимы такие функции как фильтрация данных, сортировка, поиск и т.д., жмите на "Показать интерактивную таблицу"
        </p>
        <p>
            <a href="#" id="show-dynamic" class="btn btn-primary" data-direction="{{ direction.id }}">Показать интерактивную таблицу</a>
        </p>

        {% include 'pages/documents/table.twig' %}
        <p>
            {% if paginator.getTotalPages() > 1 %}
        <nav aria-label="Page navigation">
            <ul class="pagination">
                {% if paginator.hasPreviousPage() %}
                    <li class="page-item">
                        <a class="page-link" href="?page={{ paginator.getCurrentPage() - 1 }}" aria-label="Previous">
                            <span aria-hidden="true">&laquo;</span>
                        </a>
                    </li>
                {% endif %}

                {% for page in paginator.getPages() %}
                    <li class="page-item {{ page == paginator.getCurrentPage() ? 'active' : '' }}">
                        <a class="page-link" href="?page={{ page }}">{{ page }}</a>
                    </li>
                {% endfor %}

                {% if paginator.hasNextPage() %}
                    <li class="page-item">
                        <a class="page-link" href="?page={{ paginator.getCurrentPage() + 1 }}" aria-label="Next">
                            <span aria-hidden="true">&raquo;</span>
                        </a>
                    </li>
                {% endif %}
            </ul>
        </nav>
        {% endif %}
        </p>
    </div>

{% endblock %}
